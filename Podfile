# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'qa-application' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for qa-application
  pod 'AlamofireObjectMapper', '~> 5.0'
  pod 'Alamofire'
  pod 'SwiftLint'
  pod 'lottie-ios'
  pod 'SwiftyJSON', '~> 4.0'

  target 'qa-applicationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'qa-applicationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
